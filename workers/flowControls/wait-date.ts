// import { Data } from "../receiver";
import { FlowNode } from "../../types";

export const waitDate = async (data: FlowNode) => {
  // console.log("waiting till data", data.data.selectedValue?.["wait-date"]);
  // let now = new Date();
  // let date = new Date(data.data.selectedValue["wait-date"]);
  // let diff = Math.abs(date.getTime() - now.getTime());
  // console.log("waiting for ", diff, "milliseconds");
  // setTimeout(() => {
  //   console.log("wait date completed");
  // }, diff);

  console.log("wait date");
};
